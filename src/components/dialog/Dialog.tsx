import { Typography } from '@mui/material';

export const Dialog = () => {
  return (
    <Typography variant="h4" sx={{ padding: 2, height: '89vh' }}>
      Здесь будет диалог
    </Typography>
  );
};
